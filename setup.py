from setuptools import find_packages, setup
setup(
    name='rec-browser-behave',
    version='0.0.1',
    description='To recording the browser in Thread',
    author='Alex Villanova',
    license='MIT',
    packages=find_packages(include=['video-recorder'])
)